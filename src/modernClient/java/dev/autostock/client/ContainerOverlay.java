package dev.autostock.client;

/** World-space container markers use the legacy render pipeline and are disabled on 26.x. */
public final class ContainerOverlay {
    private ContainerOverlay() { }
    static void register(ClientDraft draft) { }
}
